# Ignore file for oxfmt (reads .gitignore + .prettierignore by default).
#
# tests/expected/ holds canonical output snapshots for sync-catalog.mjs — the
# determinism test asserts sync-catalog's produced bytes match this file
# exactly. oxfmt has its own opinions about markdown layout that don't match
# what sync-catalog emits, so a format-pass here would break the test.
# This file is regenerated by re-running sync-catalog after intentional
# renderer changes.
tests/expected/
